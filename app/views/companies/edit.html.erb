<div class="container py-5">
  <h3 class="mb-3 border-bottom pb-3">Edit Company</h3>
  <%= link_to '< Company', company_path(@company), class: 'badge badge-success p-2' %>
  <%= render 'form' %>
</div>
